version: '3.7'

services:
  entry:
    image: test-server
    build: ../../server
    networks:
      - mynetwork
    ports:
      - 8080:8080
  target:
    image: test-server
    build: ../../server
    networks:
      - mynetwork

networks:
  mynetwork:
